import type { ReactNode } from "react";

export type CiProfileMenuMessages = {
  profileMenuLabel: string;
  profileTooltip: ReactNode;
  accountLabel: ReactNode;
  teamLabel: ReactNode;
  inviteUsersLabel: ReactNode;
  newTeamLabel: ReactNode;
  githubLabel: ReactNode;
  supportLabel: ReactNode;
  apiLabel: ReactNode;
  logoutLabel: ReactNode;
};