import type { ComponentType, ReactNode } from "react";
/**
 * Supported icon input for dashboard cards.
 *
 * Consumers may provide:
 * - a ready React node
 * - a React component type
 */
export type CiDashboardIcon = ReactNode | ComponentType<{
    className?: string;
}>;
//# sourceMappingURL=CiDashboardIcon.d.ts.map