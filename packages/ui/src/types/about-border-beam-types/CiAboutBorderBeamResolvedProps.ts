import type { BorderBeamProps } from "@ci-ui/types";

export type CiAboutBorderBeamResolvedProps = {
  title?: string;
  primaryText?: string;
  secondaryText?: string;
  // options?: Record<string, unknown>;
  options?: BorderBeamProps;
};
