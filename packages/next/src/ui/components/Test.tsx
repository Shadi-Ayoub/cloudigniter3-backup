// import { getSettings } from '../server';

// export default async function Test() {
//   const settings = await getSettings({ key: 'email.emailSender' });
//   return <p>{JSON.stringify(settings)}</p>;
// }
