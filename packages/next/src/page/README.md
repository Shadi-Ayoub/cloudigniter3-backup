# Page package split

- `@cloudigniter/core/page` owns framework-neutral page contracts and reusable shell/header/error components.
- `@cloudigniter/next/page` owns Next.js composition: i18n provider, breadcrumb navigation, tracing usage, and resolved Next/AWS runtime config.
