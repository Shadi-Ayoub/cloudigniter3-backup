/**
 * Registry for added Iconify icons to me merged with
 * CloudIgniter built-in icons.
 */
export type AppIconRegistry = Record<string, string>;

export const appIconRegistry: AppIconRegistry = {
  "app:reports": "mdi:chart-box-outline",
  "app:students": "mdi:school-outline",
};
