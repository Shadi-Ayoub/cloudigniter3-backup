import { components, formFields } from "./customization";
import configurations from "./configurations";
import services from "./services";

const authenticatorProps = {
  components,
  formFields,
  services,
  configurations,
};

export default authenticatorProps;
